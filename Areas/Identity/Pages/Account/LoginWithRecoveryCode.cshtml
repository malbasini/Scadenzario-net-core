@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verifica del codice di recupero";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>
    Avete richiesto di accedere con un codice di recupero. Questo accesso non verrà ricordato finché non si fornirà
    un codice dell'app Autenticator al momento dell'accesso o si disattiva il 2FA e si accede di nuovo.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="RecoveryCode" />
                <label asp-for="Input.RecoveryCode" class="form-label"></label>
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Log in</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}