@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dati personali";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Il vostro account contiene i dati personali che ci avete fornito. Questa pagina consente di scaricare o cancellare tali dati.</p>
        <p>
            <strong>L'eliminazione di questi dati comporta la rimozione definitiva dell'account, che non può essere recuperato.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post">
            <br/>
            <button class="btn btn-primary" type="submit">Scarica</button>
        </form>
        <p>
            <br/>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Cancella</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
